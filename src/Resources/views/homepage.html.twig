{% extends "layout.html.twig" %}

{% block content %}
<div class="page-header">
    <h1>Karakterek</h1>
</div>

<table class="table table-striped datatable">
    <thead>
        <tr>
            <th>Név</th>
            <th>Szint</th>
            <th>Kaszt</th>
            <td></td>
        </tr>
    </thead>
    <tbody>
    {% for c in characters %}
        <tr>
            <td><a href="{{ path("character", { "id": c.id }) }}">{{ c.name }}</a></td>
            <td>{{ c.level }}</td>
            <td>{{ c.vocation }}</td>
            <td></td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}
