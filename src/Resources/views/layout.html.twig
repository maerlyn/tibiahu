<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
<head>
    <meta charset="utf8" />
    <title>Tibiahu minimál</title>
    <link rel="stylesheet" href="{{ app.request.basepath }}/css/bootstrap.css" />
    <link rel="stylesheet" href="{{ app.request.basepath }}/css/main.css" />
    {% block head %}{% endblock %}
</head>
<body>
<div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
        <div class="container">
            <a class="brand" href="{{ path("homepage") }}">Tibiahu minimál</a>

            {% if app.session.has("admin_authenticated") %}
                <ul class="nav">
                    {# ilyen egy menupont: <li><a href="{{ path("admin_harmadikbadge") }}">Harmadik badge</a></li> #}
                </ul>

                <ul class="nav pull-right">
                    <li><a href="{{ path("admin_logout") }}">Kijelentkezés</a></li>
                </ul>
            {% endif %}
        </div>
    </div>
</div>

<div class="container">
    {% block content %}{% endblock %}
</div>
</body>
</html>
